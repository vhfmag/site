image: node:9

pages:
  script:
  - npm install --unsafe-perm now
  - yarn run build
  artifacts:
    paths:
    - public
  cache:
    paths:
    - node_modules
  only:
  - master

now:
  script:
  - npm install --unsafe-perm now
  - yarn run deploy-ci
  artifacts:
    paths:
    - public
  cache:
    paths:
    - node_modules
  only:
  - master