image: node:9

stages:
  - build
  - deploy

build:
  script:
  - yarn && yarn run build
  artifacts:
    paths:
    - public
  cache:
    paths:
    - node_modules
    - .cache
    - public
  only:
  - master

pages:
  script:
  - echo 'done!'
  artifacts:
    paths:
    - public
  only:
  - master

now:
  script:
  - npm install --unsafe-perm now
  - yarn run deploy-ci
  only:
  - master