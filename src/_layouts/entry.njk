---
layout: default
mainClass: entry
htmlClass: h-entry
---

{% macro listAuthors(authors) %}
    {% for author in authors %}
        {% if author.url %}
            <a href="{{author.url}}" class="p-author h-card u-url" itemprop="">{{ author.name }}</a>
        {% else %}
            <span class="p-author h-card" itemprop="">{{ author.name }}</span>
        {% endif %}
    {% endfor %}
{% endmacro %}

<article>
    <header>
        <h1 class="post-title p-name" itemprop="headline title">
            <a class="u-url u-uid" href="{{ page.url }}">{{ title }}</a>
        </h1>
        {% if description %}
            <p itemprop="alternativeHeadline">{{ description }}</p>
        {% endif %}
        <div class="metadata" data-separator="—">
            <span>
                <span class="p-category">Post</span> por {{ listAuthors(authors if authors else [me]) }}
            </span>
            <span>
                Publicado em
                <time class="dt-published" itemprop="datePublished pubdate" datetime="{{ date | formatDateISO }}">
                    {{ date | formatDate }}
                </time>
            </span>
            <span>{{ templateContent | wordcount }} palavras, <abbr aria-label="aproximadamente" title="aproximadamente">~</abbr>
                <span itemprop="timeRequired">{{ content | readingTime(raw=true) }} minutos</span> de leitura</span>
        </div>
    </header>

    <section class="e-content">{{ content | safe }}</section>
</article>

<link rel="stylesheet" href="/css/entry.css"/>

<link rel="stylesheet" href="/css/prism.css"/>