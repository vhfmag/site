diff --git a/node_modules/posthtml-img-autosize/lib/imgAutosize.js b/node_modules/posthtml-img-autosize/lib/imgAutosize.js
index 0d6ac52..7a248d9 100644
--- a/node_modules/posthtml-img-autosize/lib/imgAutosize.js
+++ b/node_modules/posthtml-img-autosize/lib/imgAutosize.js
@@ -55,6 +55,8 @@ module.exports = {
                 promises.push(
                     getImageDimensions(imagePath)
                         .then(dimensions => {
+                            if (!dimensions) return;
+
                             dimensions = ratio(
                                 { width: img.attrs.width, height: img.attrs.height },
                                 dimensions
@@ -68,13 +70,16 @@ module.exports = {
                                 img.attrs.height = dimensions.height;
                             }
                         })
-                        .catch(e => Promise.reject(e.message))
+                        .catch(e => console.error('Erro ao dimensionar', { imagePath }, e))
                 );
 
                 return img;
             });
 
-            return Promise.all(promises).then(() => tree);
+            return Promise.all(promises).then(() => tree).catch(error => {
+                console.error(error);
+                throw error;
+            });
         };
     }
 };
@@ -120,7 +125,13 @@ function getImageDimensions(imgPath) {
 
             response
                 .on('data', chunk => chunks.push(chunk))
-                .on('end', () => resolve(imageSize(Buffer.concat(chunks))));
+                .on('end', () => {
+                    try {
+                        resolve(imageSize(Buffer.concat(chunks)))
+                    } catch (error) {
+                        console.error('Erro ao dimensionar (sem host)', { imgPath }, error)
+                    }
+                });
         }).on('error', reject);
     });
 }
